import 'package:flutter/material.dart';

class RepoItem extends StatelessWidget {
  const RepoItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
